import { BookIcon } from 'lucide-react';

export default function MenuTitle() {
  return (
    <h4 className='flex items-center'>
      <BookIcon size={40} className='text-primary pr-2' /> BookDash
    </h4>
  );
}
